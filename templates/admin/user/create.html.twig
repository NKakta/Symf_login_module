{% extends 'layouts/admin-base.html.twig' %}

{% block title %} Vartotojų kūrimas {% endblock %}

{% block body %}
    <div class="container register-form">
        <div class="note">
            <p>Sukurti naują vartotoją</p>
        </div>
        {{ form_start(form) }}
        <div class="form-content">
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_row(form.firstName) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.lastName) }}
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_row(form.username) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.phoneNum) }}
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_row(form.plainPassword) }}
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        {{ form_row(form.email) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.credits) }}
                    </div>
                    <div class="form-group">
                        {{ form_row(form.roles) }}
                    </div>
                </div>
            </div>
            <button type="submit" class="btnSubmit">Sukurti</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}