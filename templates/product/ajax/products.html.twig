{% for product in products %}
    <div class="col-md-6 mt-3">
        <div class="product-photo">
            {% if product[0].getInStock > 0 %}
                <div class="hovered-photo-bg checkout-btn"
                     photo="{{ asset('assets/images/products/' ~ product[0].photoFilename) }}"
                     product-id="{{ product[0].id }}"
                     price="{{ product[0].price }}"
                     product-name="{{ product[0].name }}"
                     in-stock="{{ product['accountCount'] }}"
                >
                    <p>Buy now</p>
                </div>
            {% else %}

                <div class="hovered-photo-bg">
                    <p>Out of stock</p>
                </div>
            {% endif %}
            <img class="img-fluid" src="{{ asset('assets/images/products/' ~ product[0].photoFilename) }}" width="100%">
        </div>
        <div class="row product-details">
            <div class="col-md-8">
                <h4 class="mb-0 title">{{ product[0].name }}</h4>
                <h4 class="mb-0 stock">In stock: <span>{{ product['accountCount'] }}</span></h4>
            </div>
            <div class="col-md-4">
                <h1 class="mb-0 price text-right">{{ product[0].price }}$</h1>
            </div>
        </div>
    </div>
{% endfor %}

{% if products|length < 1 %}
    <div class="col-md-12">
        <div class="alert alert-danger d-block w-100" role="alert">No products in this category.</div>
    </div>
{% endif %}
