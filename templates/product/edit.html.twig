{% extends 'layouts/admin-base.html.twig' %}

{% block title %} Products {% endblock %}

{% block body %}
    <a type="button" class="btn btn-sm btn-outline-secondary" href="{{ path('product_index') }}">Back to Products list</a>
    {{ form_start(form)}}
    {{ form_row(form.name) }}
    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
    <button type="submit">Submit</button>
    {{ form_end(form) }}
{% endblock %}
