<nav id="navbar" class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        <a href="{{ path('app_homepage') }}" class="navbar-brand">Meistru sistema</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('app_homepage') }}" class="nav-link">Namai</a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a href="{{ path('admin_home') }}" class= "nav-link">Valdymo skydas</a>
                    </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link">Atsijungti</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link">Prisijungti</a>
                    </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') == false %}
                    <li class="nav-item">
                        <a href="{{ path('app_register') }}" class="nav-link">Registruotis</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
